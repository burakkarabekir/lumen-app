# TEMPLATE. Copy this file to `keystore.properties` (which is gitignored) and fill in
# the real values there. This file is tracked by git — never put real passwords here.
storeFile=keystore/lumen-release.jks
storePassword=CHANGE_ME
keyAlias=lumen-app
keyPassword=CHANGE_ME
